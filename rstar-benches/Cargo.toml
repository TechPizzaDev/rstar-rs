[package]
name = "rstar-benches"
version = "0.1.1"
authors = ["Stefan Altmayer <stoeoef@gmail.com>", "The Georust Developers <mods@georust.org>"]
edition = "2021"
rust-version = "1.68"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }
rand = { version = "0.10", features = ["min_const_gen"] }
rand_hc = "0.5"
rstar = { path = "../rstar" }

[[bench]]
name = "benchmarks"
harness = false
